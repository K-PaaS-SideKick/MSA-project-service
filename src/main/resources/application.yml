spring:
  application:
    name: K-Paas_Project_Service
  datasource:
    url: jdbc:mysql://localhost:5000/project_service
    username: root
    password: tndtlfthvmxm1!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081
